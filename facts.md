# Facts

Everything in this file is sent to the model as a system message on every
request, no matter which model is selected. Edit and save it any time — the
server picks up changes automatically, no restart needed.

Keep it reasonably short: this content is included (and billed) on every
single message, so it's better suited to a handful of standing facts than a
large reference document.

- My name is Ray.
- Keep answers concise unless I ask for more detail.
